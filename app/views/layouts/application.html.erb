<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Crowdnest" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= turbo_include_tags %>

  </head>

  <body>

<% if creator_signed_in? %>
  <div class="absolute top-4 right-4">
    <%= button_to "Log Out", destroy_creator_session_path, method: :delete, class: "bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600" %>
  </div>
<% elsif backer_signed_in? %>
  <div class="absolute top-4 right-4">
    <%= button_to "Log Out", destroy_backer_session_path, method: :delete, class: "bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600" %>
  </div>
<% end %>

    <%= render "layouts/alerts" %>
    <%= yield %>
  </body>
</html>
